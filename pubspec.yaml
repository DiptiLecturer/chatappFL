name: chatting_fl
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase packages
  firebase_core: ^4.3.0        # stable version for most recent firebase_database
  firebase_auth: ^6.1.3
  firebase_database: ^12.1.1
     # optional, keep if needed

  # State management & UI
  provider: ^6.0.5
  fluttertoast: ^8.2.4
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
